server:
  port: 8761
spring:
  application:
    name: eurekaCalcServer

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:9000/eureka/
  server:
    waitTimeInMsWhenSyncEmpty: 0
